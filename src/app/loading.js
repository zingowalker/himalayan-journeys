const Loading = () => {
  return (
    <section className="flex justify-center items-center h-screen">
      <div className="dark:text-white text-lg">Loading...</div>
    </section>
  );
};

export default Loading;
